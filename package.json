{
  "name": "my-memos",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @my-memos/app dev",
    "dev:storybook": "pnpm --filter @my-memos/ui dev",
    "build": "pnpm --filter @my-memos/app build",
    "d1:migrate:local": "wrangler d1 migrations apply DB --local --persist-to apps/memos/.wrangler/state --config apps/memos/wrangler.jsonc",
    "d1:migrate:remote": "wrangler d1 migrations apply DB --remote --config apps/memos/wrangler.jsonc",
    "deploy": "pnpm --filter @my-memos/app exec void deploy",
    "check": "pnpm --filter @my-memos/app prepare && vp check && svelte-check --workspace packages/ui --tsconfig tsconfig.json && svelte-check --workspace apps/memos --tsconfig tsconfig.check.json && tsgo --noEmit",
    "lint": "vp lint",
    "format": "vp fmt"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "7.2.0",
    "@tailwindcss/vite": "4.3.3",
    "@types/node": "26.1.2",
    "@typescript/native-preview": "beta",
    "svelte": "5.56.8",
    "svelte-check": "4.7.4",
    "tailwindcss": "4.3.3",
    "typescript": "6.0.3",
    "vite-plus": "0.2.6",
    "wrangler": "4.116.0"
  },
  "packageManager": "pnpm@11.18.0"
}
