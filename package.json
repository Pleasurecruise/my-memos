{
  "name": "my-memos",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vp run --filter ./apps/memos dev",
    "dev:storybook": "vp run --filter ./packages/ui dev",
    "build": "vp run --filter ./apps/memos build",
    "d1:migrate:local": "wrangler d1 migrations apply DB --local --persist-to apps/memos/.wrangler/state --config apps/memos/wrangler.json",
    "d1:migrate:remote": "wrangler d1 migrations apply DB --remote --config apps/memos/wrangler.json",
    "deploy": "pnpm --filter @my-memos/app exec void deploy",
    "test": "vp run -r test",
    "check": "vp run --filter ./apps/memos prepare && vp check && svelte-check --workspace packages/ui --tsconfig tsconfig.json && svelte-check --workspace apps/memos --tsconfig tsconfig.check.json && tsgo --noEmit",
    "lint": "vp lint",
    "format": "vp fmt"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "7.3.0",
    "@tailwindcss/vite": "4.3.3",
    "@types/node": "26.2.0",
    "@typescript/native-preview": "beta",
    "svelte": "5.56.9",
    "svelte-check": "4.7.6",
    "tailwindcss": "4.3.3",
    "typescript": "6.0.3",
    "vite-plus": "0.2.9",
    "wrangler": "4.124.0"
  },
  "packageManager": "pnpm@11.21.0"
}
